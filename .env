# Copy this file to ".env" and fill in real values. Never commit the real .env.

# --- File locations (defaults shown; override if your files live elsewhere) ---
EXCEL_PATH=data/participants.xlsx
PPTX_TEMPLATE_PATH=data/certificate_template.pptx
EMAIL_WITH_TIME_TXT=data/email_with_time.txt
EMAIL_WITHOUT_TIME_TXT=data/email_without_time.txt
OUTPUT_DIR=output

# --- SMTP settings ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=sunchasers20@gmail.com
SMTP_PASSWORD=kbqq gutr gpgr lspy
SMTP_FROM_NAME=sunchasers

EMAIL_SUBJECT_WITH_TIME=Your Race Certificate
EMAIL_SUBJECT_WITHOUT_TIME=Your Race Update

# --- LibreOffice binary used for pptx -> pdf conversion (only if CONVERSION_METHOD=libreoffice, or as auto-fallback) ---
SOFFICE_BIN=soffice

# --- PDF conversion method: auto | powerpoint | libreoffice ---
CONVERSION_METHOD=auto

# --- Safety switch: keep "true" until you've verified a test run ---
DRY_RUN=true
